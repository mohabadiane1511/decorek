# Le contexte de construction est la racine : on n'y envoie que le strict nécessaire.
node_modules
**/node_modules
backend/dist
backend/src/generated
.output
.vinxi
.nitro
.wrangler
dist

# `public` reste dans le contexte : l'image du site en a besoin. L'image de l'API ne le
# copie pas, chaque Dockerfile ne prenant que ce qui le concerne.
e2e
test-results
playwright-report
docs
.lovable
.git

.env
.env.*
!.env.example
*.log
